import VerifyDevice from "@/components/auth/verify-device";

const page = () => {
  return (
    <div className="bg-background min-h-screen grid place-items-center">
      <VerifyDevice />
    </div>
  );
};

export default page;
